import { PropsAlign } from '../forms';

export declare interface DividerOptions {
  /**
   * 是否虚线
   * @default
   * ```false```
   */
  dashed: boolean;
  /**
   * 分割线标题的位置
   * @default
   * ```center```
   */
  orientation: PropsAlign;
}
